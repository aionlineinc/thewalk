// Placeholder module boundary for CMS.
//
// Existing implementation remains under `src/lib/cms/*` and `src/lib/directus.ts`.
// We’ll migrate callers to import via this module as we refactor.
export * from "@/lib/cms";

